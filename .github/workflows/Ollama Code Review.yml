#name: Ollama Code Review
#
#on:
#  pull_request:
#    types: [opened, synchronize]
#
#jobs:
#  request-review:
#    runs-on: AWS-GPU # Assumes your GPU server is configured as a self-hosted runner
#
#    steps:
#    - name: Checkout repository
#      uses: actions/checkout@v4
#
#    - name: Run Ollama Code Review
#      uses: ./
#      with:
#        OLLAMA_API_URL: 'http://192.168.1.1044345345:11434' # Local Ollama server
#        MY_GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
#        OWNER: ${{ github.repository_owner }}
#        REPO: ${{ github.event.repository.name }}
#        PR_NUMBER: ${{ github.event.pull_request.number }}
#        RESPONSE_LANGUAGE: 'Russian'
#        MODEL: 'deepseek-r1:14b'
##        TRANSLATION_MODEL: 'exaone3.5:7.8b'